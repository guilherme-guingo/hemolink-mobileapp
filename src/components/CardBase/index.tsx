// import { View } from "react-native";

// interface PropsCardBase {
//   dados: object;
// }

// export const CardBase = ({ dados }: PropsCardBase) => {
//   return (
//     <View>
//       <View>
//         {dados.image && (
//           <Image
//             style={[styles.imagem, sourceStyleAdd]}
//             source={{ uri: dados.image }}
//           />
//         )}
//       </View>
//       <View>
//         {dados.name && <Text>{dados.name}</Text>}
//         {dados.cnpj && <Text>{dados.cnpj}</Text>}
//         {dados.address && <Text>{dados.address}</Text>}
//         {dados.city && <Text>{dados.city}</Text>}
//         {dados.state && <Text>{dados.state}</Text>}
//         {dados.cep && <Text>{dados.cep}</Text>}
//         {dados.phone && <Text>{dados.phone}</Text>}
//         {dados.email && <Text>{dados.email}</Text>}
//         {dados.website && <Text>{dados.website}</Text>}
//         {dados.openingHours && <Text>{dados.openingHours}</Text>}
//         {dados.bloodStock && <Text>{dados.bloodStock}</Text>}
//       </View>
//     </View>
//   );
// };
