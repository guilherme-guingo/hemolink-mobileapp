import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  cardTabs: {
    width: 80,
    alignItems: "center",
    marginTop: 30,
    height: 50,
    borderRadius: 8,
    justifyContent: "center",
  },

  textTabs: {
    marginTop: 2,
    fontSize: 12,
    fontWeight: 500,
    color: "#5C5F60",
  },
});
